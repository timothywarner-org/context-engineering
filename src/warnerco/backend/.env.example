# ============================================================================
# WARNERCO Robotics Schematica - Environment Configuration
# ============================================================================
# Copy this file to .env and fill in your values.
#
# Data Store Architecture:
#   1. JSON schematics (source of truth) - Always used, stored in data/schematics/
#   2. Chroma vectors (local) - For local semantic search, stored in data/chroma/
#   3. Azure AI Search (cloud) - For enterprise deployment
# ============================================================================

# ----------------------------------------------------------------------------
# MEMORY BACKEND SELECTION
# ----------------------------------------------------------------------------
# Options: json | chroma | azure_search
#   - json: Simple keyword search, no vectors (fastest startup)
#   - chroma: Local vector search with embeddings (recommended for local dev)
#   - azure_search: Azure AI Search for production/cloud deployment
MEMORY_BACKEND=chroma

# ----------------------------------------------------------------------------
# SERVER CONFIGURATION
# ----------------------------------------------------------------------------
HOST=0.0.0.0
PORT=8000
DEBUG=true

# ----------------------------------------------------------------------------
# AZURE AI SEARCH (required if MEMORY_BACKEND=azure_search)
# ----------------------------------------------------------------------------
# Get these from: Azure Portal > warnerco-search > Settings > Keys
#
# Endpoint: https://warnerco-search.search.windows.net
# Admin Key: Found in Settings > Keys > Primary admin key
# ----------------------------------------------------------------------------
AZURE_SEARCH_ENDPOINT=https://warnerco-search.search.windows.net
AZURE_SEARCH_KEY=
AZURE_SEARCH_INDEX=warnerco-schematics

# ----------------------------------------------------------------------------
# AZURE OPENAI (for embeddings in LangGraph flow)
# ----------------------------------------------------------------------------
# Get these from: Azure Portal > warnerco-openai > Resource Management > Keys and Endpoint
#
# Endpoint: https://warnerco-openai.openai.azure.com/
# Key: Found in Keys and Endpoint > KEY 1 or KEY 2
# Deployment: text-embedding-ada-002 (already deployed)
# API Version: Use a recent stable version
# ----------------------------------------------------------------------------
AZURE_OPENAI_ENDPOINT=https://warnerco-openai.openai.azure.com/
AZURE_OPENAI_API_KEY=
AZURE_OPENAI_DEPLOYMENT=text-embedding-ada-002
AZURE_OPENAI_API_VERSION=2024-08-01-preview

# ----------------------------------------------------------------------------
# OPENAI (alternative to Azure OpenAI)
# ----------------------------------------------------------------------------
# If you prefer OpenAI directly instead of Azure OpenAI:
# Get your API key from: https://platform.openai.com/api-keys
# ----------------------------------------------------------------------------
OPENAI_API_KEY=

# ----------------------------------------------------------------------------
# LOCAL STORAGE PATHS
# ----------------------------------------------------------------------------
# Relative to the backend directory (or absolute paths)
CHROMA_PERSIST_DIR=./data/chroma
JSON_SCHEMATICS_PATH=./data/schematics/schematics.json

# ----------------------------------------------------------------------------
# TELEMETRY
# ----------------------------------------------------------------------------
TELEMETRY_ENABLED=true
